phase: A
window_length: 60
horizon: 12
horizon_weighting: uniform
training_stride: 1    # Control: full overlap (876K training windows)
batch_size: 256
learning_rate: 0.001
optimizer: adam
num_epochs: 30
seed: 42
model_class: model.body.gru_encoder.GRUEncoder
run_name: stride_s1_control
notes: "Stride experiment control: training_stride=1, val_stride=60. Baseline for overlap harm."
